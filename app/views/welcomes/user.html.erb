<div class="row" id="main_row_ct">
   <div class="small-12 columns">

      <div class="profile-card">
        <%= render 'user/user_header_row' %>

        <% if @spot %>
          <%= render 'user/chart_row' %>
        <% end %>
      </div>

      <%= render 'user/user_footer_row' %>
   </div>

  <%= render 'user/updateSpotModal' %>
</div>